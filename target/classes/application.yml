spring:
  datasource:
    url: jdbc:mysql://localhost:3306/HM?createDatabaseIfNotExist=true
    username: root
    password: Mysql_80
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
      ddl-auto: create
  application:
    name: profile

server:
    port: 9192

